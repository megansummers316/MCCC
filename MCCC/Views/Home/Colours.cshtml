@{
	ViewData["Title"] = "Colours";
}
@model List<Colour>

<div class="text-center">
	<h1 class="display-4">Colours</h1>
</div>

<div class="colourGrid">
	@foreach (var item in Model)
	{
		<div class="colourCard">
			<h3>@item.Name</h3>
			<img src="~/img/colours/@item.Image" />
		</div>
	}
</div>